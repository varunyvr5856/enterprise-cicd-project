name: Enterprose Bnaking CI

on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      -uses: actions/checkout@v4

      -name: Say Hello
       run: echo "Enterprise Banking CI started"


